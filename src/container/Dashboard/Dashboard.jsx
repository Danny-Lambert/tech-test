import React, { useEffect, useState } from 'react';
import WeatherApi from '../../component/WeatherApi/WeatherApi';


const Dashboard = () => {

    // return props.beers.map((weather) => {
        return(
            <>
            <WeatherApi />
            </>
        )}
    ;

export default Dashboard;